{
  "buildCommand": "npx prisma generate && npm run build",
  "crons": [
    {
      "path": "/api/cron/check-alerts",
      "schedule": "0 7 * * *"
    }
  ],
  "git": {
    "deploymentEnabled": false
  }
}
